inherits: base.yaml

experiment:
  name: ablation_encoder_avg
  mode: cross_subject

model:
  encoder_type: NCT_C
  subject_conditioning: auto

data:
  avg: true

ubp:
  target_space: curriculum
  mode: curriculum
